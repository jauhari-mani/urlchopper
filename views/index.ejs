<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UrlChOpPeR</title>
</head>
<body>
    <div class="container">
        <div class="title">
            <h1 class="text-center" style="font-size: 100px; color: chocolate; padding-bottom: 0%;">Url<small style="color: black; font-size: 40px;">ChOpPeR</small></h1>
        </div>
        <form action="/tinyUrl" method="post" style="border: 50px; padding-bottom: 5%; padding-top: 5%; padding-inline-start: 5%; padding-inline-end: 5%;">
            <div class="form-floating mb-3">
                <input required type="url" class="form-control" id="url" placeholder="URL" name="url" aria-describedby="urlguide">
                <label for="url">URL</label>
                <small id="urlguide" class="form-text text-muted"> *required</small>
              </div>
              <div class="form-floating">
                <input type="text" class = "form-control" id="customkey" placeholder="Tiny Key" name="customkey">
                <label for="customkey">Tiny Key</label>
            </div>
            <div class="row" style="border: 50px; padding-bottom: 2%; padding-top: 2%; padding-inline-start: 1%; padding-inline-end: 5%;">
                <button type="submit" class="btn btn-outline-success" style="height: 40px; width: 120px;">SHORTEN</button>
            </div>
        </form>

        <div class="alert alert-warning alert-dismissible fade <%= keyExists ? "show" : "" %>" role="alert">
            <%= message %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

      <table class="table table-dark table-striped table-hover table-responsive" style="padding-inline-start: 20%; padding-inline-end: 20%;">
            <thead>
                <tr>
                    <th>FULL URL</th>
                    <th>TINY URL</th>
                    <th>DELETE</th>
                </tr>
            </thead>
            <tbody>
                <% tinyUrl.forEach(tinyUrl => { %>
                    <tr>
                        <td><a href="<%= tinyUrl.full %>"><%= tinyUrl.full %></a></td>
                        <td><a href="<%= tinyUrl.tiny %>"><%= tinyUrl.tiny %></a></td>
                        <td>
                            <form action="/delete/<%= tinyUrl.tiny %>" method="get">
                                <button type="submit" class="btn btn-danger">REMOVE</button>
                            </form>
                        </td>
                    </tr>
                <% }) %> 
            </tbody>
        </table>
    </div>
</body>
</html>

